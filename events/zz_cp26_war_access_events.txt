namespace = cp26_war_access

# Root = country
# From = war
country_event = {
	id = cp26_war_access.1
	hide_window = yes
	is_triggered_only = yes

	immediate = {
		save_event_target_as = cp26_war_access_country
		from = {
			every_war_participant = {
				limit = {
					event_target:cp26_war_access_country = {
						is_at_war_with = prev
					}
				}
				event_target:cp26_war_access_country = {
					set_closed_borders = {
						who = prev
						status = no
						forced = 35
					}
				}
				set_closed_borders = {
					who = event_target:cp26_war_access_country
					status = no
					forced = 35
				}
			}
		}
		every_country = {
			limit = {
				is_country_type = neutral_zone
			}
			set_closed_borders = {
				who = event_target:cp26_war_access_country
				status = no
				forced = 35
			}
		}
	}
}

# Keeps active wars traversable, including wars already running when CP26 is loaded.
# CP26 2026-05-14 : déclenché mensuellement (vs daily) avec forced=35 pour couvrir
# le cycle de 30 jours + 5 de buffer. cf zz_cp26_war_access_on_actions.txt.
country_event = {
	id = cp26_war_access.2
	hide_window = yes
	is_triggered_only = yes

	trigger = {
		is_at_war = yes
	}

	immediate = {
		save_event_target_as = cp26_war_access_country
		every_war = {
			every_war_participant = {
				limit = {
					event_target:cp26_war_access_country = {
						is_at_war_with = prev
					}
				}
				event_target:cp26_war_access_country = {
					set_closed_borders = {
						who = prev
						status = no
						forced = 35
					}
				}
				set_closed_borders = {
					who = event_target:cp26_war_access_country
					status = no
					forced = 35
				}
			}
		}
		every_country = {
			limit = {
				is_country_type = neutral_zone
			}
			set_closed_borders = {
				who = event_target:cp26_war_access_country
				status = no
				forced = 35
			}
		}
	}
}
