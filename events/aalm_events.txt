namespace = aa_lm

event = {
	id = aa_lm.1000
	hide_window = yes
	is_triggered_only = yes

	immediate = {
		random_galaxy_planet = {
			limit = {
				has_planet_flag = moon_luna
			}
			change_pc = pc_barren_cold_d_luna
			set_planet_entity = {
				entity = cold_barren_planet_luna_entity
				picture = pc_luna
			}
			clear_deposits = yes
			add_deposit = d_underworld_caverns
			add_deposit = d_ore_rich_slopes

			set_owner = event_target:ufop_country
			set_colony_type = col_default
			add_building = building_capital
			
			create_pop = { species = owner_main_species }
			create_pop = { species = owner_main_species }
			create_pop = { species = owner_main_species }
			create_pop = { species = owner_main_species }
			set_planet_flag = research_habitat
			# CP26 fix (2026-05-14) : on NE rajoute PLUS le modifier ici.
			# Vanilla events/game_start.txt:1360 l'applique déjà à Luna, et
			# notre zzz_cp26_aalunmars_static_modifiers.txt override directement
			# `lunar_one_colony` avec les stats. Ajouter ici en plus créait
			# une 2e icône sur la planète.
			add_district_and_planet_size_if_needed_effect = {
				district = district_city
			}
		}
		random_galaxy_planet = {
			limit = {
				has_planet_flag = planet_mars
			}
			save_event_target_as = et_planetmars
			change_pc = pc_barren_cold_d_mars
			set_planet_entity = {
				entity = barren_planet_mars_entity
				picture = pc_mars
			}
			clear_deposits = yes
			add_deposit = d_underworld_caverns
			add_deposit = d_underworld_caverns
			add_deposit = d_high_winds
			add_deposit = d_frozen_lakes
			add_deposit = d_infertile_land
			add_deposit = d_ore_rich_slopes
			
			set_owner = event_target:ufop_country
			set_colony_type = col_default
			add_building = building_capital
			
			create_pop = { species = owner_main_species }
			create_pop = { species = owner_main_species }
			create_pop = { species = owner_main_species }
			create_pop = { species = owner_main_species }
			create_pop = { species = owner_main_species }
			set_planet_flag = mining_habitat
			add_district_and_planet_size_if_needed_effect = {
				district = district_industrial
			}
			add_district_and_planet_size_if_needed_effect = {
				district = district_city
			}
			
		}
	}
}
