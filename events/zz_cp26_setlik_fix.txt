namespace = cp26_setlik

country_event = {
	id = cp26_setlik.1
	hide_window = yes
	is_triggered_only = yes
	fire_only_once = yes

	trigger = {
		is_ufop = yes
	}

	immediate = {
		random_system = {
			limit = { has_star_flag = setlik }
			set_star_flag = outpost
			set_star_flag = ufop_system
			set_star_flag = ufop
			set_star_flag = setlik
			if = {
				limit = { exists = starbase }
				starbase = { set_owner = root }
			}
			else = {
				create_starbase = {
					size = starbase_outpost
					owner = root
				}
			}
		}
	}
}

country_event = {
	id = cp26_setlik.2
	hide_window = yes
	is_triggered_only = yes
	fire_only_once = yes

	trigger = {
		is_ufop = yes
	}

	immediate = {
		random_system = {
			limit = { has_star_flag = terra_nova_system }
			set_star_flag = outpost
			set_star_flag = ufop_system
			set_star_flag = ufop
			set_star_flag = terra_nova_system
			if = {
				limit = { exists = starbase }
				starbase = { set_owner = root }
			}
			else = {
				create_starbase = {
					size = starbase_outpost
					owner = root
				}
			}
		}
	}
}

country_event = {
	id = cp26_setlik.3
	hide_window = yes
	is_triggered_only = yes
	fire_only_once = yes

	trigger = {
		is_klingon = yes
	}

	immediate = {
		random_galaxy_planet = {
			limit = { has_planet_flag = planet_rura_penthe }
			create_rura_penthe_colony = yes
			solar_system = {
				set_star_flag = outpost
				set_star_flag = klingon_system
				set_star_flag = klingon
				set_star_flag = rura_penthe_system
				if = {
					limit = { exists = starbase }
					starbase = { set_owner = root }
				}
				else = {
					create_starbase = {
						size = starbase_outpost
						owner = root
					}
				}
			}
		}
	}
}

country_event = {
	id = cp26_setlik.4
	hide_window = yes
	is_triggered_only = yes
	fire_only_once = yes

	trigger = {
		has_country_flag = ferengi
	}

	immediate = {
		every_country = {
			if = {
				limit = { has_country_flag = cardassia }
				save_event_target_as = cardassian_country
			}
			if = {
				limit = { has_country_flag = tholian }
				save_event_target_as = tholian_country
			}
		}

			random_galaxy_planet = {
				limit = { has_planet_flag = ferengi_capital }
				create_ferenginar_colony = yes
				root = {
					set_adjective = "Ferengi"
					set_custom_capital_location = prev
				}
				root = {
					give_technology = { tech = tech_destroyer_unlock message = no }
				}
				solar_system = {
					set_surveyed = { surveyed = yes surveyor = root }
					set_star_flag = outpost
				set_star_flag = ferengi_home_system
				set_star_flag = ferengi_system
				set_star_flag = ferengi
				if = {
					limit = { exists = starbase }
					starbase = { set_owner = root }
				}
				else = {
					create_starbase = {
						size = starbase_starport
						owner = root
					}
				}
			}
		}

		if = {
			limit = {
				is_ai = no
				exists = event_target:cardassian_country
			}
			establish_communications_no_message = event_target:cardassian_country
		}
		if = {
			limit = {
				is_ai = no
				exists = event_target:tholian_country
			}
			establish_communications_no_message = event_target:tholian_country
		}

		if = {
			limit = {
				NOT = { any_owned_fleet = { is_ship_class = shipclass_constructor } }
			}
			create_civilian_fleet = yes
		}

		if = {
			limit = {
				NOT = { any_owned_fleet = { is_ship_class = shipclass_military } }
			}
			create_fleet = {
				set_take_point = yes
				effect = {
					create_ship = {
						name = random
						random_existing_design = ferengicorvette
						initial_officers_perc = 0.6
					}
					create_ship = {
						name = random
						random_existing_design = ferengi_destroyer
						initial_officers_perc = 0.2
					}
					set_location = root.capital_star
				}
			}
		}
	}
}
